/* code here for Take a Number Feature */

var katzDeliLine = [];

function takeANumber(katzDeliLine, name) {
  for (var i = 0; i < array.length; i++) {
    array[i]
  }
  return "Welcome `${name}`. You are number `${}`"
}
